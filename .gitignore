# build artifacts
*.o
i225nvm

# device dumps / backups produced at runtime
backups/
backup_*.bin
*.bin
*.eep
*.rom

# local firmware checkouts / staging directories
firmware/
firmware-src/

# local OS metadata
.DS_Store

# Intel's proprietary NVM update binary — not redistributable
nvmupdate64e
